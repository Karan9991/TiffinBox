<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tiffinbox">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".Seller.Recipe"></activity>
        <activity
            android:name=".Seller.AddView"
            android:label="@string/title_activity_add_view"
            android:theme="@style/AppTheme">

        </activity>
        <activity android:name=".Customer" />
        <activity android:name=".Authentication.SignIn">

        </activity>
        <activity android:name=".Authentication.Register">  <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>